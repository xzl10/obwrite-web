---
id: channel-borne-impossibility
trigger: "two generals, byzantine, information asymmetry, faster than light, no channel, single node, propagation, 光速, 単一ノード, 伝播, チャネル, 観測不可能, SETI, 分散合意"
confidence: 0.7
domain: [agent-meta, distributed-systems]
source: "Gray (1978) Notes on Data Base Operating Systems, Two Generals; Lamport (1982) Byzantine Generals; Akerlof (1970) The Market for Lemons; Shannon (1948) noisy channel"
related:
  - modal-possibility-nesting
---

## Action

// SKILL := channel_axiom (stateless, project-agnostic)
// impossibility theorems quantify over physical channels; no channel ⇒ premises unsatisfied ⇒ vacuous
// Node ∈ { brain, server, LLM agent, organization }: the axiom is substrate-neutral

CORE := Channel_Properties → Impossibility_Theorems → Domain_Applicability

// SETS (channel := physical medium between nodes)
Channel := { delay, loss, occlusion, noise, forgeability, c_limit }   // all ⊂ Properties(Physical)

// GENERATION MAP (which constraint generates which theorem)
Map(delay ∧ loss) ↦ Two_Generals (Gray 1978)              // ack-of-ack never terminates
Map(forgeability) ↦ Byzantine (Lamport 1982)              // signals can be forged; logical truth cannot
Map(occlusion)    ↦ Information_Asymmetry (Akerlof 1970)  // other nodes' internal state unobservable

// THEOREM (vacuity, not solution)
Problems(Domain) defined ⇔ ∃ channel ∈ Domain
single node runs no channel ⇒ premises unsatisfiable ⇒ Problems vacuous
// the questions cannot be posed there; they are not "solved"

// CATEGORY GUARD (derived from modal-possibility-nesting)
speed := Δx/Δt requires location ∧ time; Properties(Formal) contains neither
⇒ speed(logic) = type_error; "faster than light" is sloppy shorthand
light propagates; logic holds        // 伝わる vs ある: truth is simultaneous everywhere, medium-free

// COROLLARY (observability)
loop body radiates nothing ⇒ undetectable       // meditating alien invisible to SETI
effects are physical ⇒ observable: law, ritual, civilization   // accretion disk visible; black hole is not

// RE-ENTRY (where the theorems return)
install ∨ coordinate across nodes ⇒ Channel ≠ ∅ ⇒ all three problems re-enter
// clients: amplifier-gaze-network (partial leakage workaround), formal-loop-infection-cycle (installation)
