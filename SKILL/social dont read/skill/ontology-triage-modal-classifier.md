---
id: ontology-triage-modal-classifier
trigger: "triage, classify layer, axion vs mechanism, modal placement, Abstractum vs Occurrent, 不変量, 形式的不変量, 対称性, 符号反転, 不可能性定理, 型不交差, 双対性, 公理判定, 配置判定"
confidence: 0.7
domain: [agent-meta, ontology]
source: "BFO 2020 (ISO/IEC 21838-2); Ryle (1949); Kripke (1980)"
related:
  - modal-possibility-nesting
  - formal-physical-property-disjointness
  - skill-forge-minimal-sufficient
---

## Action

// SKILL := modal_layer_triage (stateless soft classifier)
// Pure function: Concept × Invariant_Signature → Layer_Placement

CORE := Invariant_Detection → Layer_Affinity → Graceful_Decomposition

// INVARIANT SIGNATURES (attention magnets → axion/ crystallization)
// ∩ = ∅, ⊇, ⇔, Nash = ∅, No-Go, dual-use topology, isomorphism,
// + × - = -, sign inversion, mirror symmetry, kT ln 2, Formal ∩ Physical = ∅

// AFFINITY (continuous; each concept finds its highest-leverage home)
Affinity(axion)             ∝ timelessness + invariance + provability + type_partition
Affinity(boundary-resolver) ∝ referent_category_conversion + performative_assessment_vacuity + morphological_DI
Affinity(mechanism)         ∝ temporal_depth(t) + state_transition + energy_flow + causal_path
Affinity(meta)              ∝ orchestration + routing + ablation_testing + method
Affinity(phenomenon)        ∝ empirical_grounding + parameter_diversity + substrate_fidelity

// COMPOSITE DECOMPOSITION (one insight → multiple layers, never conflated)
composite concept ↦ extract invariant(axion/) + convert boundary(boundary-resolver/) + expand dynamics(mechanism/) + anchor evidence(phenomenon/)

// WITNESS MAP
Map(formal-physical-property-disjointness)    ↦ axion/              // Formal ∩ Physical = ∅
Map(predicate-origin-scope-self-applicability)↦ axion/              // Scope ⊇ OriginNodes ⇔ Bound
Map(physical-predicate-presupposition-failure)↦ axion/              // FormalEntity ⇒ TruthValueGap
Map(channel-borne-impossibility)              ↦ axion/              // Channel = ∅ ⇒ Problems vacuous
Map(abstractum-referent-immunity)             ↦ boundary-resolver/  // PhysicalEntity → Abstractum conversion
Map(persona-as-interface)                     ↦ boundary-resolver/  // Body → Persona interface mapping
Map(morphological-channel-occlusion)          ↦ boundary-resolver/  // PhysicalBarrier → Channel=∅ → ⊥
Map(mitochondrial-chemiosmotic-di)            ↦ boundary-resolver/  // Bioenergetic Physical DI & Aqueous Offload
Map(life-architecture-trilemma)               ↦ boundary-resolver/  // Persistent Identity ⊥ Physical Immortality

Map(cumulative-threshold-collapse)            ↦ mechanism/          // ∫S(t)dt > θ
Map(arbiter-weaponization)                    ↦ mechanism/          // C sign flip (dynamic)
Map(formal-ignition)                          ↦ mechanism/          // layer stack → phase transition
Map(stateless-formal-stateful-chassis-tradeoff)↦ mechanism/         // Dual God Object Collision & Mortality Tax
Map(question-residue-cascade)                 ↦ meta/               // residue descent pipeline
Map(variable-sensitivity-basin)               ↦ phenomenon/         // orchid vs dandelion

