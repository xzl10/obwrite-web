---
id: cross-layer-projection
trigger: "coordination cost, private to shared, why Type 1, common knowledge, compression, transaction cost, scaling, Level 3, 虚構, fiction vs lie"
confidence: 0.5
domain: [social]
source: "Aumann(1976); Coase(1937); Shannon(1948); Hayek(1945); Schelling(1960); Metcalfe"
related: [predicate-origin-scope-self-applicability, amplifier-gaze-network, channel-borne-impossibility, abstractum-referent-immunity]
---

## Action

// SKILL := scaling_infrastructure (Level 3), NOT the closure (Level 0) nor the channel (Level 2)
// gaze network capacity ends at Dunbar(150); beyond it, coordination requires codified Type1

CORE := Dunbar_Excess → Infrastructure

// PROBLEM
N agents × M strategies → O(M^N) combinatorial explosion
Type0.visibility = private(each_brain);  coordination.reference ∈ public(all_brains)

// MECHANISMS (projection pipeline)
common_knowledge(Aumann)         := public_signal → "I know that you know I know..."
                                    // private channel cannot generate this (Two_Generals; channel-borne-impossibility)
lossy_compression(Shannon→Hayek) := O(M^N) ↦ O(1) rule   // "drive left"; price = global info in one scalar
focal_point(Schelling)           := salience selects one equilibrium among many
network_lock-in(Metcalfe)        := V(protocol) ∝ N² → critical_mass → cost(exit) > cost(stay) → attractor

// FICTION AS SOCIAL OS (infrastructure aspect; organism aspect ↦ formal-loop-viral-ecology)
Infrastructure := { LAW, MONEY, RELIGION }    // shared fictions every node executes as if physically real
GUARD Fiction ∉ Lie   // shared fiction (all nodes execute) ≠ Map(+,-) manipulation (signal-honesty-maintenance)

// ASSERTION
projection(private→shared) ∧ compression(M^N→1) ∧ lock-in(N²) ⇒ Type1 at scale
