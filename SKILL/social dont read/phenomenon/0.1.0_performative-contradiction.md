---
id: performative-contradiction
trigger: "double standard, hypocrisy, 建前 vs 本音, why social rules contradict"
confidence: 0.5
domain: [social]
source: "Price(1970); Russell(1908); Merton(1949); Anderson(1983); Dunbar(1992)"
related: [russell-price-pipeline, baptist-bootlegger, rule-installation-paradox]
---

## Action

// WHY: two games with different rules run on ONE human interface

LAYER_0 := { Continuant, Price T1, Latent, Local }  // ≡ Type0
  // physical flesh; boundary maintenance; maximizes own resources
LAYER_1 := { Abstractum, Price T2, Manifest, Global }  // ≡ Type1
  // imagined protocol (law, nation); cross-node integration; maximizes group throughput

ASSERT disjoint(LAYER_0, LAYER_1)  // Russell/Ryle category mistake

human ⊢ LAYER_0 ∧ LAYER_1 → performative_contradiction
  // SYNTAX(LAYER_1) declares Term_2 (global optimum) as manifest output
  // SEMANTICS(LAYER_0) executes Term_1 (local optimum) as latent payoff
